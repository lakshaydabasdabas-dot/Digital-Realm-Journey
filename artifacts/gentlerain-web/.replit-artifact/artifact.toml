kind = "web"
previewPath = "/"
title = "Gentlerain — Interactive Experience"
version = "1.0.0"
id = "artifacts/gentlerain-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22198

[services.development]
run = "pnpm --filter @workspace/gentlerain-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gentlerain-web", "run", "build" ]
publicDir = "artifacts/gentlerain-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22198"
BASE_PATH = "/"
